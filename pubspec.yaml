name: practice
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks:
  flutter_riverpod:
  riverpod_annotation:
  go_router:
  percent_indicator:
  freezed_annotation:
  google_fonts:
  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  freezed:
  build_runner:
  riverpod_generator:
  json_serializable:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/google_fonts/